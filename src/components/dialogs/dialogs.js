import React from "react";
import s from './dialogs.module.css';

const Dialogs = (props) => {
    return (
        <div>
            Dialogs
        </div>
    );
};

export default Dialogs;